/* FireFly 5 — sincroniza economia, pedidos e missões */
import {save} from './save.js';import {ensureEconomy} from './economy.js';import {addQuestProgress} from './quests.js';import {addOrderProgress} from './orders.js';
let previousResources={},previousCatches={},previousSold=0,booted=false;function snapshot(){previousResources={...(save.progression?.resources||{})};previousCatches={...(save.catches||{})};previousSold=save.economy?.totalSold||0;booted=true;}
export function updateFireFly5(){ensureEconomy();if(!booted){snapshot();return;}for(const [id,n] of Object.entries(save.progression?.resources||{})){const delta=n-(previousResources[id]||0);if(delta>0){addOrderProgress('resource',id,delta);addQuestProgress('resource',id,delta);}}for(const [key,n] of Object.entries(save.catches||{})){const delta=n-(previousCatches[key]||0);if(delta>0){const type=key.split(':')[0];addOrderProgress('catch',type,delta);addQuestProgress('catch',type,delta);}}const sold=(save.economy?.totalSold||0)-previousSold;if(sold>0)addQuestProgress('sell','sell',sold);snapshot();}
