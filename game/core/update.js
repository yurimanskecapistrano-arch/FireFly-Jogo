/* =========================================================
   UPDATE 4.1 — mundo vivo e captura 2.0
   ========================================================= */
import {state,addParticles,tickParticles,tickFade,isNight} from './state.js';import {clamp} from './utils.js';import {isDown} from './input.js';import {ui} from './dom.js';import {W} from './constants.js';import {updatePlayer} from '../entities/player.js';import {updateCreature} from '../entities/creatures.js';import {tickFishing} from '../systems/fishing.js';import {tickCapture} from '../systems/capture-2.js';import {AudioManager} from '../systems/audio.js';import {updateResources,ensureProgression,progressSummary} from '../systems/progression.js';import {save,saveGame} from '../systems/save.js';import {updateLivingWorld,tickWorldEvent,clockLabel} from '../systems/living-world.js';import {updateWeather,weatherType,weatherLabel,weatherModifier} from '../systems/weather.js';import {updateExploration,currentRegion} from '../systems/exploration.js';import {completeVanTravel} from '../entities/van.js';import {spawnRegionalWildlife,updateRegionalWildlife} from '../systems/capture-2-world.js';
let lastNight=null,lastMap=null,autosave=0,stepTimer=0,spawnTimer=0;
export function update(dt){state.t+=dt;state.clock=(state.clock+dt/300)%1;ensureProgression();save.progression.clock=state.clock;const night=isNight();if(ui.time)ui.time.textContent=`${night?'☾':'☀'} ${clockLabel()} · ${night?'NOITE':'DIA'} · ${weatherLabel()}`;if(night!==lastNight){lastNight=night;AudioManager.setTime(night);}if(state.map!==lastMap){lastMap=state.map;AudioManager.playMusic(state.map);spawnTimer=0;}updateWeather(dt);updateLivingWorld(dt);tickWorldEvent(dt);updateExploration();const maxX=updatePlayer(dt),p=state.player;state.camera.target=clamp(p.x-W*.46,0,Math.max(0,maxX-W+100));state.camera.x+=(state.camera.target-state.camera.x)*Math.min(1,dt*4.5);state.camera.shake*=Math.pow(.08,dt);const world={playerX:p.x,playerY:p.y,isNight:night,maxX,worldHour:clockLabel(),event:state.worldEvent,entities:state.entities,weather:weatherType(),weatherModifier};for(const entity of state.entities){updateCreature(entity,dt,world);if(entity.type==='rare'){const rareVisible=state.map==='cave'?!!save.inventory.lantern:(night||weatherType()==='rain'||save.inventory.luckyCharm>0);entity.visible=rareVisible;entity.mood=rareVisible?'alert':'hidden';}if(weatherType()==='rain'&&entity.type==='butterfly')entity.vx*=Math.max(0,1-dt*3);}updateRegionalWildlife(dt);spawnTimer+=dt;if((state.map==='forest'||state.map==='cave')&&spawnTimer>4){spawnTimer=0;spawnRegionalWildlife();}tickFishing(dt,isDown(' '));tickCapture(dt);updateResources(dt);stepTimer+=dt;if(stepTimer>.55&&Math.abs(p.vx)>80){stepTimer=0;save.progression.stats.steps++;}autosave+=dt;if(autosave>20){autosave=0;saveGame();}if(state.travel){state.travel-=dt;if(state.travel<.65)addParticles(p.x,p.y,'#d9ded0',2);if(state.travel<=0)completeVanTravel();}tickParticles(dt);tickFade(dt);const ps=progressSummary();if(ui.level)ui.level.textContent=`LV ${ps.level}`;if(ui.prompt&&state.map==='forest'){const r=currentRegion();if(r&&Math.abs(p.x-r.from)<8)ui.prompt.textContent=r.name;}}
